const assert = require('assert');
const Background = require('../background.js');

describe('Background', () => {
  it('has a test', () => {
    assert(false, 'Background should have a test');
  });
});
